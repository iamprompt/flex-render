{
  "name": "flex-render-api-example",
  "private": true,
  "version": "0.0.6",
  "scripts": {
    "dev": "tsup --watch --clean --onSuccess 'node dist/index.js'",
    "build": "tsup",
    "prepublish": "tsup",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src/"
  },
  "devDependencies": {
    "@iamprompt/eslint-config": "workspace:^",
    "@iamprompt/typescript-config": "workspace:^",
    "@types/express": "^4.17.21",
    "tsup": "^8.1.0",
    "typescript": "^5.4.5"
  },
  "dependencies": {
    "express": "^4.19.2",
    "flex-render": "workspace:^"
  }
}
